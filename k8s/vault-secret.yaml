apiVersion: v1
kind: Secret
metadata:
  name: vault-credentials
  namespace: default
type: Opaque
stringData:
  VAULT_ADDRESS: "http://vault-service:8200"
  VAULT_TOKEN: "ENCRYPTED_VAULT_TOKEN_HERE"
  VAULT_MOUNT_POINT: "secret"
  VAULT_SECRET_PATH: "microservices"
